import { ProfileSettingsForm } from "@/components/auth/profile-settings-form";

export const metadata = {
  title: "Profile Settings | GoSupportMe",
};

export default function ProfileSettingsPage() {
  return <ProfileSettingsForm />;
}
