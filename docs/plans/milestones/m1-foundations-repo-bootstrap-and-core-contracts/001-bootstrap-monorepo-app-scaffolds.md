# Task 001: Bootstrap Monorepo App Scaffolds

## Goal
Initialize frontend and API project scaffolds with baseline routing, health, and middleware foundations.

## Deliverables
- [ ] `apps/web/*` initialized with responsive layout shell and route scaffolds for fundraiser/community/profile
- [ ] `apps/api/*` initialized with health check endpoint, request ID middleware, and logging/tracing scaffolding

## Notes
Follow the selected Phase 0 architecture stack and keep structure consistent with planned monorepo layout.

## Verification
Start both services locally and confirm scaffolded routes/health endpoints render expected base responses.
