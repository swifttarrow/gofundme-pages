# Task 001: Build Fundraiser Page and Donation Module

## Goal
Implement fundraiser content, trust context, and a clear donation UI that always shows the real charged total.

## Deliverables
- [ ] `apps/web/src/app/fundraiser/[id]/page.tsx` renders hero, story, organizer/trust modules, and donation feed
- [ ] `apps/web/src/components/donation-module.tsx` supports suggested amounts, custom amount, tip selector (`0/5/10/15/20/custom`), and explicit total charged display

## Notes
Prioritize mobile-safe layout and accessibility semantics in donation controls to support checkpoint requirements.

## Verification
Run accessibility checks and manually complete donation interactions at desktop and `320px` widths without UI regressions.
