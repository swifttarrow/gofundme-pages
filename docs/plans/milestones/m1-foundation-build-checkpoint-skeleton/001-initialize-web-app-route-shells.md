# Task 001: Initialize web app route shells

## Goal
Create responsive route skeletons for fundraiser, community, and profile pages in the Next.js app.

## Deliverables
- [ ] `apps/web` app scaffold exists with App Router enabled.
- [ ] Route files exist for `/f/[slug]`, `/communities/[slug]`, and `/u/[handle]`.
- [ ] Shared navigation and responsive layout primitives are wired into page shells.

## Notes
Follow route and SSR-first decisions from the plan, and keep layout parity-ready for later Pencil implementation.

## Verification
Run the app locally and manually confirm each route renders a non-error responsive shell on mobile and desktop breakpoints.
